spring:
  datasource:
    url: ${CUENTAS_DB_URL}
    username: ${CUENTAS_DB_USER}
    password: ${CUENTAS_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  sql:
    init:
      mode: never
